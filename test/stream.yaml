module: io.stream
dependencies: 
  - io.buffer
class: Stream
description: "
This class represents a data stream of arbitrary bytes.
"
functions:
  - constructor:
  - constructor:
  - destructor:
  - method: foo
    description: "This function will do a thing"
    parameters:
    - type: int
      name: x
      description: "The first thing to input."
    - type: int
      name: a
      description: "The second thing to input."
