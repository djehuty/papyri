module: geometry.point
dependencies: 
class: Point
description: "Represents a single point in a 2d space."
functions:
  - constructor: "Constructs a point representing the origin (0, 0)"
    parameters:
  - constructor: "Constructs a point representing the given location."
    parameters:
    - type: double
      name: x
    - type: double
      name: y
  - property: x
    description: "The point's x coordinate."
    return-type: double
    parameters:
  - property: y
    description: "The point's y coordinate."
    return-type: double
    parameters:
